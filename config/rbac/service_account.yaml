apiVersion: v1
kind: ServiceAccount
metadata:
  name: k8s-ai-operator
  namespace: ai-operator-system
  labels:
    app.kubernetes.io/name: k8s-ai-operator
  annotations:
    # Set this to the ARN of the IAM role the operator uses to call Bedrock
    # and assume per-endpoint roles. See docs/irsa-setup.md.
    eks.amazonaws.com/role-arn: ""
